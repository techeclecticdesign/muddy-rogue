import { describe, it, expect } from "vitest";

// Simple smoke test to ensure the test setup works
describe("App", () => {
  it("should pass a basic test", () => {
    expect(true).toBe(true);
  });
});
